# Dependencies
node_modules/
.pnp/
.pnp.js

# Logs
logs/
*.log
npm-debug.log*
.server.log

# Runtime data (Railway Volume에서 영구 저장) — 루트 anchor 로 한정
/data/
/output/
/tmp/
/backups/
*.db
*.db-journal
*.db-wal
*.db-shm

# KDS 디자인 토큰·SVG 라이브러리는 git 포함 필수 (negate)
!kds-v4/kds/data/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Build / IDE
.vscode/
.idea/
.DS_Store
Thumbs.db
dist/build/
.next/
.vercel/
.railway/

# Test / temp
coverage/
.nyc_output/
test/results/
test/screenshots/

# Claude integrations (외부 PC 경로 의존 — 컨테이너 제외)
.claude/integrations/

# Bundled Claude (~/.claude → .claude/ 동기화본) — 포함
# 단 agent-memory 는 PC별 상태라 제외
.claude/agent-memory/

# KDS v4 — 코드와 SVG 라이브러리는 commit, 산출물·로그·node_modules 는 Volume/local 만
kds-v4/node_modules/
kds-v4/to-figma/
kds-v4/from-figma/
kds-v4/research/
kds-v4/logs/
kds-v4/*.log
kds-v4/bridge.log

# 민감 정보
*.pem
*.key
secrets/
